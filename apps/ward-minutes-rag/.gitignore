# 港区サイト/議会の本文を含む生成物はコミットしない（二次利用=無断転載の回避）。
# スクリプトから再生成できるため、リポジトリには含めない方針。
data/raw/*.html
data/sample/*.jsonl

# Python仮想環境
.venv/
__pycache__/
*.pyc

# 埋め込みインデックス（chunks.json に港区サイト・議会の本文が含まれる）。
# scripts/build_index.py で再生成できるため、リポジトリには含めない。
data/index/
